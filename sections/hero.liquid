<section id="hero">
    <img class="hero__image" src="{{ settings['hero-banner'].settings.background_image | image_url: '100%' | prepend: 'assets/' }}" alt="Ket Club" width="1200" height="800">
    <button id="hero-button" class="button">{{ settings['hero-banner'].settings.button_text }}</button>
</section>
